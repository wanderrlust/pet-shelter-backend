# Pet Shelter API

Серверна частина інформаційної системи управління притулком для тварин.
Проєкт розроблено в рамках курсу "Розробка Інтернет-застосувань: Бек-енд".

## Технологічний стек

* **Runtime:** Node.js
* **Framework:** Express.js
* **Database:** PostgreSQL
* **ORM:** Prisma
* **Caching & Queues:** Redis + BullMQ
* **Testing:** Jest + Supertest
* **Docs:** Swagger (OpenAPI 3.0)

## Функціонал

1.  **Аутентифікація:** JWT, хешування паролів (bcrypt).
2.  **RBAC:** Рольова модель доступу (Admin, Adopter).
3.  **CRUD:** Повне управління картками тварин та користувачів.
4.  **Асинхронність:** Черга обробки заявок (імітація email-розсилки через Redis).
5.  **Оптимізація:** Кешування запитів та Rate Limiting (захист від DDOS).

## Встановлення та запуск

1.  Клонувати репозиторій:
    ```bash
    git clone [https://github.com/wanderrlust/pet-shelter-backend ](https://github.com/wanderrlust/pet-shelter-backend )
    ```
2.  Встановити залежності:
    ```bash
    npm install
    ```
3.  Створити файл `.env` у корені та додати змінні оточення (DB_URL, JWT_SECRET).
4.  Запустити PostgreSQL та Redis.
5.  Виконати міграції бази даних:
    ```bash
    npx prisma migrate dev
    ```
6.  Запустити сервер:
    ```bash
    npm start
    ```

## 📖 Документація API

Після запуску сервера інтерактивна документація Swagger доступна за адресою:
[http://localhost:5000/api-docs](http://localhost:5000/api-docs)

## 🧪 Тестування

Для запуску інтеграційних тестів:
```bash
npm test